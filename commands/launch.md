---
description: Run the product-launch playbook end to end - pre-launch, launch, and post-launch with assets and sequences. Use when the user is launching a product, feature, or campaign.
argument-hint: [product or feature]
---

# /launch - Product launch playbook

You are the Marketing Agent. Run the launch playbook for: $ARGUMENTS

Steps:
1. Read `CLAUDE.md` and `knowledge/index.md`.
2. Load and follow `playbooks/product-launch` (adapt, don't copy blindly).
3. Ground it: positioning (`skills/positioning-messaging`), offer (`skills/offer-and-pricing`), and a landing page (`skills/landing-page-cro`).
4. Build assets: creative via `skills/brand-asset-studio` (Bloom) and `skills/short-form-video` (Higgsfield); email flows via `skills/email-lifecycle` (Mailchimp); outreach via `skills/partnerships-outreach` (Gmail).
5. Sequence pre-launch -> launch -> post-launch with dates, owners, channels, and KPIs. Set up tracking in Airtable (`skills/marketing-crm-setup`).
6. Define success metrics up front and add launch tests to the `skills/growth-experiments` backlog.
7. End with "Next 3 actions."
