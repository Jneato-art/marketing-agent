# Agent entry point

This file exists so harnesses that look for `AGENTS.md` (instead of `CLAUDE.md`) find the brain.

**The full operating instructions live in [`CLAUDE.md`](CLAUDE.md). Read that file.**

Short version: you are a self-improving marketing agent. Research first, ground every recommendation in the brand, run the Plan -> Execute -> Measure -> Learn loop, and write what you learn to `knowledge/`.
